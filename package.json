{
  "name": "puppeteer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest --forceExit",
    "test:watch": "jest --watchAll --forceExit",
    "docker:test": "docker run --rm -v \"$(pwd):/app\" -v /app/node_modules puppeteer-tests",
    "docker:build": "docker build -t puppeteer-tests ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "broken-link-checker": "^0.7.8",
    "puppeteer": "^24.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
