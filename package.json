{
  "name": "puppeteer-headless-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --forceExit",
    "test:watch": "jest --watchAll --forceExit"
  },
  "dependencies": {
    "broken-link-checker": "^0.7.8",
    "puppeteer": "^24.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
